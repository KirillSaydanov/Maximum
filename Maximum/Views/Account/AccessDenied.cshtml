@{
    ViewData["Title"] = "Отказ в доступе";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h2 class="text-center">
                    <i class="fas fa-exclamation-triangle"></i> @ViewData["Title"]
                </h2>
            </div>
            <div class="card-body text-center">
                <p class="lead">У вас нет прав для доступа к этому ресурсу.</p>
                <p>Пожалуйста, войдите в систему с учетными данными, имеющими необходимые права.</p>
                
                <div class="mt-4">
                    <a asp-controller="Account" asp-action="Login" class="btn btn-primary">
                        <i class="fas fa-sign-in-alt"></i> Войти
                    </a>
                    <a asp-controller="Main" asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-home"></i> На главную
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
