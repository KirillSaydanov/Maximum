@{
    ViewData["Title"] = "Главная";
}

<div class="text-center">
    <h1 class="display-4">Добро пожаловать в Maximum</h1>
    
    @if (User.Identity?.IsAuthenticated == true)
    {
        <div class="alert alert-success" role="alert">
            <h4 class="alert-heading">Привет, @User.Identity.Name!</h4>
            <p>Вы успешно вошли в систему.</p>
            <hr>
            <p class="mb-0">
                <a asp-controller="Account" asp-action="Profile" class="btn btn-primary">
                    <i class="fas fa-user"></i> Перейти в профиль
                </a>
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info" role="alert">
            <h4 class="alert-heading">Добро пожаловать!</h4>
            <p>Для доступа к функциям системы необходимо войти в систему или зарегистрироваться.</p>
            <hr>
            <p class="mb-0">
                <a asp-controller="Account" asp-action="Login" class="btn btn-primary me-2">
                    <i class="fas fa-sign-in-alt"></i> Войти
                </a>
                <a asp-controller="Account" asp-action="Register" class="btn btn-success">
                    <i class="fas fa-user-plus"></i> Регистрация
                </a>
            </p>
        </div>
    }
    
    <div class="row mt-5">
        <div class="col-md-4">
            <div class="card">
                <div class="card-body text-center">
                    <i class="fas fa-shield-alt fa-3x text-primary mb-3"></i>
                    <h5 class="card-title">Безопасность</h5>
                    <p class="card-text">Надежная система аутентификации и авторизации.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-body text-center">
                    <i class="fas fa-users fa-3x text-success mb-3"></i>
                    <h5 class="card-title">Управление пользователями</h5>
                    <p class="card-text">Простое управление профилями и настройками.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-body text-center">
                    <i class="fas fa-database fa-3x text-info mb-3"></i>
                    <h5 class="card-title">PostgreSQL</h5>
                    <p class="card-text">Надежное хранение данных с использованием PostgreSQL.</p>
                </div>
            </div>
        </div>
    </div>
</div>
